# Current Directives

Top priorities surfaced in the dashboard. Edit freely — the orchestrator reads this
file live. Format: `- [P0] <title>` (tags: P0 = critical, P1/P2 = lower).

- [P0] Wire real data sources into the Jarvis dashboard
- [P1] Manual mic → STT voice test
- [P1] Make the initial git commit
